# Force LF line endings for all files on all platforms
* text=auto eol=lf

# Shell scripts must always be LF
*.sh text eol=lf

# Batch files must always be CRLF (Windows requirement)
*.bat text eol=crlf
